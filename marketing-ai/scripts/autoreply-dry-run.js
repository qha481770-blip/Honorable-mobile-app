import { config } from '../src/config.js';import { JsonStore } from '../src/store.js';import { AutoReplyEngine } from '../src/core/autoreply.js';import { MarketingResponseGenerator } from '../src/core/content.js';
const settings=config();const store=new JsonStore(settings.dataDir);const state=await store.snapshot();const latestRunToken=state.discoveryRuns.at(-1)?.runToken;const current=state.opportunities.filter(x=>x.dataType==='LIVE'&&(!latestRunToken||x.discoveryRunToken===latestRunToken));const engine=new AutoReplyEngine({store,responseGenerator:new MarketingResponseGenerator(settings.waitlistUrl),config:{...settings.autoReply,dryRun:true}});const proposals=await engine.dryRun(current);
console.log('AUTO-REPLY DRY RUN — NOTHING WAS PUBLISHED');console.log(`OUTREACH_STATUS: ${new MarketingResponseGenerator(settings.waitlistUrl).outreachStatus()}`);console.log(`PROPOSALS: ${proposals.length}`);for(const [index,x] of proposals.entries()){console.log(`\n#${index+1} ${x.platform} — AUTO_REPLY_SCORE ${x.autoReplyScore}`);console.log(`STATUS: ${x.status}`);console.log(`GATES: ${x.reasons.join(', ')||'ALL PASSED'}`);console.log(`REPLY: ${x.reply}`);console.log(`ORIGINAL: ${x.targetUrl}`);}
